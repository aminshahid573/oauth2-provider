# Application Environment: development, staging, production
APP_ENV=development

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# Logging Configuration
LOG_LEVEL=debug # debug, info, warn, error

# Database Configuration
MONGO_URI=mongodb://root:password@localhost:27017/oauth2_provider?authSource=admin
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT Configuration
# Use a strong, randomly generated secret key (e.g., openssl rand -base64 32)
JWT_SECRET_KEY=your-super-secret-key-change-me
JWT_ISSUER=oauth2-provider
JWT_ACCESS_TOKEN_LIFESPAN_MINUTES=15
JWT_REFRESH_TOKEN_LIFESPAN_HOURS=168 # 7 days

# Frontend Configuration
BASE_URL=http://localhost:8080

# CSRF Configuration
# Use a strong, 32-byte random key (e.g., openssl rand -base64 32)
CSRF_AUTH_KEY=your-32-byte-long-csrf-auth-key-change-me